swagger: "2.0"
info:
  title: Sample API
  description: API description in Markdown.
  version: 1.0.0
host: localhost:8080
basePath: /
schemes:
  - http
paths:
  /hello:
    get:
      summary: hello.
      description: hello.
      produces:
        - application/json
      responses:
        200:
          description: OK
  /json:
    get:
      summary: json.
      description: json.
      produces:
        - application/json
      responses:
        200:
          description: OK

